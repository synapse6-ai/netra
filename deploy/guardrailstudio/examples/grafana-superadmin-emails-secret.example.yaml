# kubectl create secret generic grafana-superadmin-emails -n observability \
#   --from-file=emails.txt=deploy/guardrailstudio/examples/grafana-superadmin-emails.example.txt

apiVersion: v1
kind: Secret
metadata:
  name: grafana-superadmin-emails
  namespace: observability
  labels:
    app.kubernetes.io/part-of: netra
type: Opaque
stringData:
  emails.txt: |
    admin@example.com
    ops@example.com
