# Copy to cluster — do not commit with real credentials.
# kubectl create secret generic grafana-google-oauth -n observability \
#   --from-literal=client-id='YOUR_CLIENT_ID' \
#   --from-literal=client-secret='YOUR_CLIENT_SECRET'

apiVersion: v1
kind: Secret
metadata:
  name: grafana-google-oauth
  namespace: observability
  labels:
    app.kubernetes.io/part-of: netra
type: Opaque
stringData:
  client-id: "REPLACE_ME"
  client-secret: "REPLACE_ME"
