# OS / editor
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Local AI / editor overrides
.claude/
.cursor/
*.local.json

# Secrets and credentials — never commit
.env
.env.*
!.env.example
credentials.json
*-credentials.json
*.pem
*.key
*.p12
service-account*.json
*-sa-key.json

# Customer-specific private overlays (Synapse6 central hub stays local-only)
deploy/synapse6/

# Ephemeral install / debug artifacts
*.kubeconfig
kubeconfig
.kube/
helm-charts/*.tgz
/tmp/netra-*
